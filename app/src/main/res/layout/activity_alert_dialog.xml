<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="50dp">

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/bt_close"
        android:layout_width="30dp"
        android:layout_height="30dp"
        app:layout_constraintTop_toTopOf="@id/img_flat_ilus"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@drawable/ic_close"
        android:layout_marginEnd="20dp"
        />
    <ImageView
        android:id="@+id/img_flat_ilus"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/flat_ilus_city"
        />
    <TextView
        android:id="@+id/tv_dialog_title"
        style="@style/HeaderTextBold"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/img_flat_ilus"
        tools:text="@string/select_province" />

    <SearchView
        android:id="@+id/sv_alert_dialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_start_end_variant"
        android:layout_marginEnd="@dimen/margin_start_end_variant"
        android:elevation="4dp"
        android:iconifiedByDefault="false"
        android:queryHint="Search"
        app:layout_constraintTop_toBottomOf="@id/tv_dialog_title" />

    <TextView
        android:id="@+id/tv_please_select_province"
        android:visibility="visible"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Please select province first"
        app:layout_constraintTop_toBottomOf="@id/sv_alert_dialog"
        style="@style/SubHeaderText"
        android:gravity="center"
        android:textColor="@color/blue"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <androidx.recyclerview.widget.RecyclerView
        tools:visibility="visible"
        android:id="@+id/rv_alert_dialog"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="200dp"
        android:paddingBottom="20dp"
        android:layout_marginEnd="10dp"
        android:layout_marginStart="10dp"
        tools:listitem="@layout/item_vertical_alert_dialog" />


</androidx.constraintlayout.widget.ConstraintLayout>